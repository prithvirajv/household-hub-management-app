apiVersion: v1
kind: Service
metadata:
  name: household-hub
  namespace: household-hub
spec:
  type: NodePort
  selector:
    app: household-hub
  ports:
    - name: http
      port: 80
      targetPort: 8080
